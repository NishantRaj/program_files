#include <bits/stdc++.h>
using namespace std;
#define ll long long
int n , m , b , mod;
ll dfs(int who , int error , int total_till){
	 if(error == b)
	 	return 0;
}
int main(){
	cin>>n>>m>>b>>mod;
	int error[n+9];
	for(int i = 0; i < n ; i++)
		cin>>error[i];

	queue<ll> q;

}