#include <bits/stdc++.h>
using namespace std;
bool cmp(const char &a , const char &b){
	return a > b;
}
int main(){
	int t;
	cin>>t;
	while(t--){
		int l ;
		cin>>l;
		string s;
		cin>>s;
		sort(s.begin() , s.end(), cmp);
		cout<<s<<endl;
	}
	return 0;
}
